{% extends 'base.html.twig' %}

{% block title %}Modifier un Post{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier un Post</h1>

        {{ include('post/_form.html.twig', {'button_label': 'Modifier'}) }}
        <a class="btn btn-danger" href="{{ path('app_compte_show', {'id': app.user.id}) }}">Retour</a>
    </div>
{% endblock %}
